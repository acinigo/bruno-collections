info:
  name: Who Am I
  type: http
  seq: 14

http:
  method: GET
  url: "{{url}}/api/auth/app/whoami"
  headers:
    - name: user
      value: Test
    - name: channel
      value: Postman
  auth:
    type: bearer
    token: "{{acces_token_app}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
