info:
  name: ChannelUsers
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/channelUsers/"
  headers:
    - name: tenantId
      value: "{{tenantId}}"
      disabled: true
  auth:
    type: bearer
    token: "{{acces_token_app}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
