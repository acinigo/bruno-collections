info:
  name: Makeup Model Id_1
  type: http
  seq: 8

http:
  method: PATCH
  url: "{{url}}/api/geneva/makeupmodels/650974ab5ff716b6060d3217"
  headers:
    - name: forceCalculation
      value: "true"
      disabled: true
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value:
          - /C:/Users/Milucho/Downloads/image (8).png
      - name: name
        type: text
        value: test_model2
      - name: description
        type: text
        value: test_model
  auth:
    type: bearer
    token: "{{acces_token_app}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
