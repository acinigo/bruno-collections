info:
  name: Makeup Models
  type: http
  seq: 5

http:
  method: GET
  url: "{{url}}/api/geneva/makeupmodels"
  headers:
    - name: forceCalculation
      value: "true"
      disabled: true
  body:
    type: multipart-form
  auth:
    type: bearer
    token: "{{acces_token_app}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
