info:
  name: Makeup Models_1
  type: http
  seq: 6

http:
  method: POST
  url: "{{url}}/api/geneva/makeupmodels"
  headers:
    - name: forceCalculation
      value: "true"
      disabled: true
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value:
          - /C:/Users/Milucho/Downloads/image (8).png
      - name: name
        type: text
        value: test_model
      - name: description
        type: text
        value: test_model
  auth:
    type: bearer
    token: "{{acces_token_app}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
