info:
  name: Batch end
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/api/products/batch/end"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{acces_token_app}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
