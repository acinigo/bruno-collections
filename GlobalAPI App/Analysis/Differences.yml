info:
  name: Differences
  type: http
  seq: 21

http:
  method: GET
  url: "{{url}}/api/analysis/654db45fad44ba72a80dce3f/differences"
  headers:
    - name: user
      value: "9990000000494"
    - name: channel
      value: IPSA ID
  auth:
    type: bearer
    token: "{{acces_token_app}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
