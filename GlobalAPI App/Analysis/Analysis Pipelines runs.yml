info:
  name: Analysis Pipelines runs
  type: http
  seq: 6

http:
  method: GET
  url: "{{url}}/api/analysis/690df0066897cf3b77793687/pipelines/runs"
  headers:
    - name: user
      value: IPSA ID
      disabled: true
    - name: channel
      value: guest
      disabled: true
    - name: recalculate
      value: "true"
      disabled: true
  auth:
    type: bearer
    token: "{{acces_token_app}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
