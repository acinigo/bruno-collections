info:
  name: Analysis Link
  type: http
  seq: 10

http:
  method: GET
  url: "{{url}}/api/analysis/link/"
  auth:
    type: bearer
    token: "{{acces_token_app}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
