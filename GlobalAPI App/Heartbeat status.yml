info:
  name: Heartbeat status
  type: http
  seq: 12

http:
  method: GET
  url: "{{url}}/api/heartbeat/status"
  headers:
    - name: filter
      value: code='4514254959978'
      disabled: true
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{acces_token_app}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
