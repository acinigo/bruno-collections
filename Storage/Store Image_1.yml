info:
  name: Store Image_1
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/api/storage/image/test_face/test.jpg"
  auth:
    type: bearer
    token: "{{acces_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
