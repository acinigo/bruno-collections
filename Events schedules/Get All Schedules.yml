info:
  name: Get All Schedules
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/schedules"
  auth:
    type: bearer
    token: "{{acces_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
