info:
  name: Get one Schedule
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/schedules/688b81f711d3dea766b1d4a7"
  auth:
    type: bearer
    token: "{{acces_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
