info:
  name: Chatbots
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/tenants/4223b073-b67e-45d2-8d54-d7e3098f9d32/chatbots"
  headers:
    - name: Content-Type
      value: application/json
  auth:
    type: bearer
    token: "{{acces_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
