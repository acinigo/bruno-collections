info:
  name: Change password with token
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/users"
  auth:
    type: bearer
    token: "{{acces_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
