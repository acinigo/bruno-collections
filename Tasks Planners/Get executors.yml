info:
  name: Get executors
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/planner/status"
  headers:
    - name: tenants
      value: f981c55e-d765-44d2-a1ed-417d96ecc960
      disabled: true
    - name: collections
      value: ""
      disabled: true
  auth:
    type: bearer
    token: "{{acces_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
