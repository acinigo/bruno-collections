info:
  name: Heartbeat
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/api/heartbeat/all"
  auth:
    type: bearer
    token: "{{acces_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
