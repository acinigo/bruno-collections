info:
  name: RotateTOTP
  type: http
  seq: 7

http:
  method: PUT
  url: "{{url}}/api/users/rotatetotp/example@domain.com"
  auth:
    type: bearer
    token: "{{acces_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
