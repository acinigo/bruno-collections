info:
  name: Transformers
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/tenants/f981c55e-d765-44d2-a1ed-417d96ecc960/transformers"
  auth:
    type: bearer
    token: "{{acces_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
