info:
  name: Storage management
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/storages"
  auth:
    type: bearer
    token: "{{acces_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
