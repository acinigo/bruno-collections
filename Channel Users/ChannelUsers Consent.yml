info:
  name: ChannelUsers Consent
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/api/channelUsers/"
  headers:
    - name: tenantId
      value: "{{tenantId}}"
      disabled: true
  auth:
    type: bearer
    token: "{{acces_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
