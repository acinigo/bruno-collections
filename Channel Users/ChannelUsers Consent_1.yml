info:
  name: ChannelUsers Consent_1
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/api/channelUsers/"
  headers:
    - name: tenantId
      value: "{{tenantId}}"
      disabled: true
  auth:
    type: bearer
    token: "{{acces_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
