info:
  name: ChannelUsers by channel
  type: http
  seq: 5

http:
  method: GET
  url: "{{url}}/api/channelUsers/byChannel/postman"
  headers:
    - name: tenantId
      value: "{{tenantId}}"
      disabled: true
  auth:
    type: bearer
    token: "{{acces_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
