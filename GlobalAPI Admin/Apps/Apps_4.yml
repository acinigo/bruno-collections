info:
  name: Apps_4
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{url}}/api/apps/appIdtoremove"
  auth:
    type: bearer
    token: "{{acces_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
