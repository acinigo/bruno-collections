info:
  name: Generate totp for user
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/api/users/generatetotp/63345ab4152c63def719efb2"
  auth:
    type: bearer
    token: "{{acces_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
